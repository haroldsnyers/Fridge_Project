{% extends 'base.html.twig' %}

{% block title %}Fridge inside  {% endblock %}

{% block body %}
    {{ dump() }}
    <div style="padding-bottom: 100px"></div>

    <div class="container fridgeCreationContainer">
        <h1 class="fridgeCreationTitle">Edit Fridge </h1>

        {{ include('fridge/_formFridge.html.twig', {'h2_label':'Edit your fridge info', 'button_label': 'Update'}) }}

        <a class="fridge-link" href="{{ path('fridgeRepo_show') }}">back to list</a>

{#        {{ include('fridge/_deleteFridge.html.twig') }}#}

    </div>

{% endblock %}