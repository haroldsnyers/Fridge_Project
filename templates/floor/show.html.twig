{% extends 'base.html.twig' %}

{% block title %}Floor{% endblock %}

{% block body %}
    <h1>Floor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ floor.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ floor.type }}</td>
            </tr>
            <tr>
                <th>Qty_food</th>
                <td>{{ floor.qtyFood }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('floor_index') }}">back to list</a>

    <a href="{{ path('floor_edit', {'id': floor.id}) }}">edit</a>

    {{ include('floor/_delete_form.html.twig') }}
{% endblock %}
